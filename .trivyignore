# .trivyignore
AVD-AWS-0086 # 静的ウェブホスティングを行うためfalseが必須
AVD-AWS-0087 # 静的ウェブホスティングを行うためfalseが必須
AVD-AWS-0091 # 静的ウェブホスティングを行うためfalseが必須
AVD-AWS-0093 # 静的ウェブホスティングを行うためfalseが必須
AVD-AWS-0104 # アウトバウンドはできるだけ明示的にレンジを記載すべきだが、gitの提供するipレンジの幅が広すぎで冗長になる、都度変わる可能性があるためこの脆弱性は無視する。(インバウンドで自分のIPしか通さないので基本漏洩などの心配はなし)
AVD-AWS-0088 # HTTP後悔することが前提なので暗号化の必要なし、暗号化すると静的ウェブホスティングでアクセスできなくなる
AVD-AWS-0132 # HTTP後悔することが前提なので暗号化の必要なし、暗号化すると静的ウェブホスティングでアクセスできなくなる